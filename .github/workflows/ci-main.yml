name: main branch CI

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: read

jobs:
  build:
    name: "${{ matrix.name }}"
    runs-on: "ubuntu-latest"
    concurrency:
      group: ${{ github.workflow }}-${{ github.run_id }}-${{ matrix.name }}
      cancel-in-progress: false
    container:
      image: ghcr.io/${{ github.repository_owner }}/gnuradio4-core-sdk:${{ matrix.core_sdk_tag }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "Ubuntu 24.04 | gcc-14 | Release"
            cache_key: ubuntu-2404-gcc14-release
            core_sdk_tag: main-ubuntu-24.04-gcc14-release
            cc: gcc-14
            cxx: g++-14
            cmake-build-type: Release
            cmake_flags: "-DADDRESS_SANITIZER=OFF"
          - name: "Ubuntu 26.04 | gcc | Release"
            cache_key: ubuntu-2604-gcc-release
            core_sdk_tag: main-ubuntu-26.04-gcc-release
            cc: gcc
            cxx: g++
            cmake-build-type: Release
            cmake_flags: "-DADDRESS_SANITIZER=OFF"
          - name: "Ubuntu 26.04 | gcc | Debug"
            cache_key: ubuntu-2604-gcc-debug
            core_sdk_tag: main-ubuntu-26.04-gcc-debug
            cc: gcc
            cxx: g++
            cmake-build-type: Debug
            cmake_flags: "-DADDRESS_SANITIZER=OFF"
          - name: "Ubuntu 24.04 | clang-20 | Release"
            cache_key: ubuntu-2404-clang20-release
            core_sdk_tag: main-ubuntu-24.04-clang20-release
            cc: clang-20
            cxx: clang++-20
            cmake-build-type: Release
            cmake_flags: "-DCMAKE_CXX_FLAGS=-fsanitize=address -DCMAKE_EXE_LINKER_FLAGS=-fsanitize=address -DCMAKE_SHARED_LINKER_FLAGS=-fsanitize=address"
          - name: "Fedora 44 | clang | Release"
            cache_key: fedora-44-clang-release
            core_sdk_tag: main-fedora-44-clang-release
            cc: clang
            cxx: clang++
            cmake-build-type: Release
            cmake_flags: "-DCMAKE_CXX_FLAGS=-fsanitize=address -DCMAKE_EXE_LINKER_FLAGS=-fsanitize=address -DCMAKE_SHARED_LINKER_FLAGS=-fsanitize=address"
          - name: "Fedora 44 | clang | Debug"
            cache_key: fedora-44-clang-debug
            core_sdk_tag: main-fedora-44-clang-debug
            cc: clang
            cxx: clang++
            cmake-build-type: Debug
            cmake_flags: "-DCMAKE_CXX_FLAGS=-fsanitize=address -DCMAKE_EXE_LINKER_FLAGS=-fsanitize=address -DCMAKE_SHARED_LINKER_FLAGS=-fsanitize=address"
          - name: "Ubuntu 24.04 | Emscripten | Release"
            cache_key: ubuntu-2404-emscripten-release
            core_sdk_tag: main-ubuntu-24.04-emscripten-release
            cc: emcc
            cxx: em++
            cmake_wrapper: emcmake
            cmake-build-type: Release
            cmake_flags: "-DADDRESS_SANITIZER=OFF"

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Prepare sccache usage (if available)
        id: enable_sccache
        env:
          SCCACHE_S3_ENDPOINT: ${{ vars.SCCACHE_S3_ENDPOINT }}
          SCCACHE_S3_BUCKET: ${{ vars.SCCACHE_S3_BUCKET }}
          SCCACHE_S3_REGION: ${{ vars.SCCACHE_S3_REGION }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_S3_KEY }}
          AWS_ACCESS_KEY_ID: ${{ vars.SCCACHE_S3_KEY_ID }}
        run: .github/ci-tools/setup_cmake_cache_flags.bash "${{ matrix.name }}"

      - name: Cache fetchContent
        uses: actions/cache@v5
        env:
          cache-name: cache-fetchContent-cache
        with:
          path: ${{runner.workspace}}/build/_deps
          key: fetchContent-${{ runner.os }}-${{ matrix.cache_key }}-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: |
            fetchContent-${{ runner.os }}-${{ matrix.cache_key }}

      - name: Configure
        if: matrix.cmake_wrapper == null
        shell: bash
        env:
          CC: "${{ matrix.cc }}"
          CXX: "${{ matrix.cxx }}"
        run: >
          cmake \
            -S . \
            -B ../build \
            -DCMAKE_COLOR_DIAGNOSTICS=ON \
            -DDISABLE_EXTERNAL_DEPS_WARNINGS=ON \
            -DCMAKE_BUILD_TYPE=${{ matrix.cmake-build-type }} \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DCMAKE_PREFIX_PATH=/opt/gnuradio4 \
            ${{ steps.enable_sccache.outputs.CXX_LAUNCHER }} \
            ${{ steps.enable_sccache.outputs.C_LAUNCHER }} \
            -DENABLE_COVERAGE=OFF \
            -DCMAKE_CXX_COMPILER=${{ matrix.cxx }} \
            -DCMAKE_C_COMPILER=${{ matrix.cc }} \
            ${{ matrix.cmake_flags }}

      - name: Configure CMake Emscripten
        if: matrix.cmake_wrapper == 'emcmake'
        shell: bash
        run: |
          system_node="$(command -v node)"
          emcmake cmake -S . -B ../build \
            -DCMAKE_COLOR_DIAGNOSTICS=ON \
            -DDISABLE_EXTERNAL_DEPS_WARNINGS=ON \
            -DCMAKE_BUILD_TYPE=${{ matrix.cmake-build-type }} \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DCMAKE_PREFIX_PATH=/opt/gnuradio4 \
            -DCMAKE_FIND_ROOT_PATH=/opt/gnuradio4 \
            -DCMAKE_CROSSCOMPILING_EMULATOR="${system_node}" \
            -DENABLE_COVERAGE=OFF \
            ${{ matrix.cmake_flags }}

      - name: Build
        shell: bash
        run: cmake --build ../build

      - name: execute tests
        if: matrix.cc != 'gcc-14' || matrix.cmake-build-type != 'Debug'
        env:
          DISABLE_SENSITIVE_TESTS: 1 # disables tests which are sensitive to execution speed and will not run with instrumented code
        working-directory: ${{runner.workspace}}/build
        shell: bash
        run: ctest --output-on-failure

      - name: validate installed Emscripten SDK
        if: matrix.cmake_wrapper == 'emcmake'
        shell: bash
        run: |
          system_node="$(command -v node)"
          install_prefix="${{ runner.workspace }}/install-library"
          cmake --install ../build --prefix "${install_prefix}"
          emcmake cmake -S test_external/minimal_consumer -B "${{ runner.workspace }}/build-smoke" \
            -DCMAKE_PREFIX_PATH="${install_prefix};/opt/gnuradio4" \
            -DCMAKE_FIND_ROOT_PATH="${install_prefix};/opt/gnuradio4" \
            -DCMAKE_CROSSCOMPILING_EMULATOR="${system_node}"
          cmake --build "${{ runner.workspace }}/build-smoke"
          "${system_node}" "${{ runner.workspace }}/build-smoke/gnuradio4_library_consumer.js"

      - name: Show final sccache and build directory stats
        if: always()
        continue-on-error: true
        shell: bash
        run: ./.github/ci-tools/build-summary.sh "${{ matrix.name }}" "../build"

  coverage:
    name: "GCC-14 Coverage"
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository_owner }}/gnuradio4-core-sdk:main-ubuntu-24.04-gcc14-release
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Prepare sccache usage (if available)
        id: enable_sccache
        env:
          SCCACHE_S3_ENDPOINT: ${{ vars.SCCACHE_S3_ENDPOINT }}
          SCCACHE_S3_BUCKET: ${{ vars.SCCACHE_S3_BUCKET }}
          SCCACHE_S3_REGION: ${{ vars.SCCACHE_S3_REGION }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_S3_KEY }}
          AWS_ACCESS_KEY_ID: ${{ vars.SCCACHE_S3_KEY_ID }}
        run: .github/ci-tools/setup_cmake_cache_flags.bash "Coverage"

      - name: Configure (coverage)
        shell: bash
        env:
          CC: gcc-14
          CXX: g++-14
        run: |
          cmake -S . -B ../build \
            -DCMAKE_COLOR_DIAGNOSTICS=ON \
            -DDISABLE_EXTERNAL_DEPS_WARNINGS=ON \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DCMAKE_PREFIX_PATH=/opt/gnuradio4 \
            ${{ steps.enable_sccache.outputs.CXX_LAUNCHER }} \
            ${{ steps.enable_sccache.outputs.C_LAUNCHER }} \
            -DENABLE_COVERAGE=ON \
            -DADDRESS_SANITIZER=OFF

      - name: Build
        run: cmake --build ../build

      - name: Execute tests
        env:
          DISABLE_SENSITIVE_TESTS: 1 # disables tests which are sensitive to execution speed and will not run with instrumented code
        working-directory: ${{runner.workspace}}/build
        shell: bash
        run: ctest --output-on-failure

      - name: Generate coverage report
        working-directory: ${{runner.workspace}}/build
        shell: bash
        run: cmake --build . --target coverage

      - name: Show final sccache and build directory stats
        if: always()
        continue-on-error: true
        shell: bash
        run: ./.github/ci-tools/build-summary.sh "Coverage" "../build"
